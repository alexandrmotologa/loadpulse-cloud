{
  "name": "loadpulse-cloud",
  "version": "1.0.0",
  "description": "On-demand API load testing station and Telegram Mini App companion to alexandrmotologa/loadpulse",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "build:server": "npm run build --prefix server",
    "build:web": "npm run build --prefix web",
    "build": "npm run build:web && npm run build:server",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "start": "npm run start --prefix server",
    "test": "npm run test --prefix server"
  },
  "author": "Alexandr Motologa",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/alexandrmotologa/loadpulse-cloud.git"
  }
}
